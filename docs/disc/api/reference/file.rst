``oaknut.file``
===============

Shared metadata types, sidecar / xattr / filename-encoding handling,
and the ``FSError`` base. Every other ``oaknut-*`` package depends on
this one.

.. note::

   Autodoc body coming. For the concepts these symbols implement, see
   :doc:`/api/patterns/metadata` and :doc:`/api/patterns/errors`.
